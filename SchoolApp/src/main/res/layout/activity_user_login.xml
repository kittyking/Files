<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/white_background"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.school.schoolapp.activity.login.UserLoginActivity" >

    <EditText
        android:id="@+id/phoneEditText"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/background_login_edittext"
        android:gravity="center"
        android:hint="请输入手机号"
        android:numeric="integer" />

    <EditText
        android:id="@+id/pwdEditText"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@+id/phoneEditText"
        android:layout_marginTop="15dp"
        android:background="@drawable/background_login_edittext"
        android:gravity="center"
        android:hint="请输入登录密码"
        android:password="true" />

    <Button
        android:id="@+id/loginBtn"
        android:layout_width="match_parent"
        android:layout_height="@dimen/user_login_btn_height"
        android:layout_below="@+id/pwdEditText"
        android:layout_marginTop="15dp"
        android:background="@drawable/selector_submit_background_yellow"
        android:text="登录" />

    <Button
        android:id="@+id/registerBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/loginBtn"
        android:background="@null"
        android:text="立即注册"
        android:textSize="13sp" />

    <Button
        android:id="@+id/fastLoginBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/loginBtn"
        android:background="@null"
        android:text="手机号快速登陆"
        android:textSize="13sp" />

</RelativeLayout>