<resources>

    <!-- Action bar 主题设置 -->
    <style name="actionbar_custom_style" parent="android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">#FED853</item>
        <!-- useLogo 使actionbar左侧默认菜单栏隐藏 -->
        <item name="android:displayOptions">useLogo</item>
        <!-- <item name="android:displayOptions">useLogo</item> -->
        <!-- Action bar Tab背景颜色设置 -->
        <!-- <item name="android:backgroundStacked">#11ffffff</item>  -->
    </style>
    
    <!-- 现在正在用的主题颜色 -->
    <style name="ActivityTheme_Custom" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/actionbar_custom_style</item>
    </style>
    
    <style name="RecyclerTheme" parent="Base.RecyclerTheme"></style>
    <style name="Base.RecyclerTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>  
   		<item name="windowNoTitle">true</item>  
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    
    
     <!-- 自定义仿IOS的PaySheet底部Dialog的样式 ，有模糊效果 -->
    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>
    
    <!-- PaySheet进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@animator/actionsheet_in</item>
        <item name="android:windowExitAnimation">@animator/actionsheet_out</item>
    </style>
    
    

    
    
    <!-- photopicker 顶部标题栏标题文本样式 -->
    <style name="topbarTittleTheme">
        <item name="android:textColor">@color/topbar_btn_text_color</item>
        <!--
        <item name="android:shadowColor">@color/gray</item>
        <item name="android:shadowDy">0.5</item>
        <item name="android:shadowRadius">1</item>
        -->
    </style>
    
</resources>
